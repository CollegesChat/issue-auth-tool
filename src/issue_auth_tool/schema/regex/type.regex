(?x)^                            # 从行首开始
(
    view                      # 匹配 'view'
    (?:\s+\d+)+               # 至少一个空格+数字
  | google                    # 或者匹配 'google'
    (?:\s+[\u4e00-\u9fff]+){2} # 后面跟两个中文词
)
$                             # 到行尾结束